"""ByteNoise: Convert any file's raw bytes into sound.

A Windows desktop application for transforming arbitrary files into audible
output via three synthesis modes (Raw Noise, Drone, Crunch) and a chain of
configurable effects.
"""

__version__ = "2.0.0"
